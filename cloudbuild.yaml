steps:
# build image
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', '${_IMAGE_NAME}', '.']
  
# push image
- name: 'gcr.io/cloud-builders/docker'
  args: ['push', '${_IMAGE_NAME}']

# deploy image
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args:
  - 'run'
  - 'deploy'
  - 'logbook-script'
  - '--service-account=cocatrip@ct-master.iam.gserviceaccount.com'
  - '--image=${_IMAGE_NAME}'
  - '--region=asia-southeast1'
  - '--allow-unauthenticated'
  - '--port=8080'

# delete older image
- name: 'asia-docker.pkg.dev/gcr-cleaner/gcr-cleaner/gcr-cleaner-cli'
  args:
  - '--repo=asia-southeast1-docker.pkg.dev/${PROJECT_ID}/ct-docker/logbook-script'
  - '--keep=1'

images:
- '${_IMAGE_NAME}'

substitutions:
  _IMAGE_NAME: 'asia-southeast1-docker.pkg.dev/${PROJECT_ID}/ct-docker/logbook-script:${COMMIT_SHA}'

options:
  logging: CLOUD_LOGGING_ONLY
  dynamic_substitutions: true
