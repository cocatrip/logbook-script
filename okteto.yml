name: logbook-script

build:
  app:
    context: .
    dockerfile: Dockerfile

deploy:
  - helm upgrade --install logbook-script chart --set image.repository=${OKTETO_BUILD_APP_REGISTRY}/${OKTETO_BUILD_APP_REPOSITORY} --set image.tag="okteto"
